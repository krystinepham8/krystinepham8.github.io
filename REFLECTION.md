Homework 1: 
1. The computer’s browser needs the website’s HTML and CSS to load. So it needs to ask Github for it by creating a connection. Github then gives those files to the browser so it can show the website to the user. 

2. Docker can run programs in a container. That means their program is isolated from the rest of the machine. On Docker, people can run multiple programs on the same machine. Github can host websites but it cannot run programs like docker.


Homework 2: 
1. Flex-direction: row means that the items in the container is arranged in a row (horizontally). Flex-direction: column is the items in the container being arranged in a column (vertically). So they are different in the way they organize/ arrange the items in the flex box.
2. It is important to use relative units when creating a responsive design because screen sizes change. The pixel size of one device is different from the other, so we shouldn't really use pixel size. We should use relative units like % so it can scale to the different screen sizes of different devices. 
