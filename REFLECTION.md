1. The computer’s browser needs the website’s HTML and CSS to load. So it needs to ask Github for it by creating a connection. Github then gives those files to the browser so it can show the website to the user. 

2. Docker can run programs in a container. That means their program is isolated from the rest of the machine. On Docker, people can run multiple programs on the same machine. Github can host websites but it cannot run programs like docker.
